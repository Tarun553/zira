"use client"
import React from 'react'

const SprintCreationForm = ({ projectTitle, projectId,
    projectKey,
    sprintKey }: { projectTitle: string, projectId: string,
        projectKey: string,
        sprintKey: number }) => {
    return (
        <>
        <div>
            
            </div>
        </>
    )
}

export default SprintCreationForm